# src/backend/services/tts_service.py

def text_to_speech(text: str, output_file_path: str) -> None:
    """
    Convierte texto a audio y guarda el archivo resultante.
    """
    # Ejemplo (no implementado):
    # import some_tts_library
    # audio_data = some_tts_library.synthesize(text)
    # with open(output_file_path, "wb") as f:
    #     f.write(audio_data)
    pass
